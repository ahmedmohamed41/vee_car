import 'package:flutter/material.dart';

class CarFlowScreen extends StatelessWidget {
  const CarFlowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}